version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: sermi-store-mongo-dev
    ports:
      - "27017:27017"
    volumes:
      - mongo_dev_data:/data/db

volumes:
  mongo_dev_data:
